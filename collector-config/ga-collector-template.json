{
  "dataType": "{{dataGroup + dataType}}",
  "query": {
    "id": "ga:{{ga_id}}",
    "metrics": {{metrics}},
    "dimensions": {{dimensions}},
    "filters": {{filters}},
    "plugins": [
        "gds.ga.plugins.scraperwiki:compute_department",
        "gds.ga.plugins.scraperwiki:aggregate_department"
    ]
  },
  "target": {
    "url": "{{backdrop_target}}/gov_uk_content_{{dataGroup + dataType}}",
    "token": "{{bearer_token}}"
  }
}