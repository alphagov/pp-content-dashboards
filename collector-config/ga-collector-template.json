{
  "dataType": "{{dataGroup}}_{{dataType}}",
  "query": {
    "id": "ga:{{ga_id}}",
    "metrics": {{metrics}},
    "dimensions": {{dimensions}},
    "filters": {{filters}},
    "plugins": [
        "gds.ga.plugins.scraperwiki:compute_department",
        "gds.ga.plugins.scraperwiki:aggregate_department"
    ]
  },
  "target": {
    "url": "{{backdrop_target}}/{{dataGroup}}/{{dataType}}",
    "token": "{{bearer_token}}"
  }
}
